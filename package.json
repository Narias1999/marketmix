{
  "name": "markertmix",
  "version": "0.1.0",
  "description": "Recruting test",
  "main": "server.js",
  "scripts": {
    "lint": "eslint --fix .",
    "install-all": "npm install && cd ./api && npm install && cd ./../db/ && npm install && cd ./../services npm install",
    "setup-db": "node ./db/setup.js",
    "start-dev": "nodemon api/server.js",
    "start": "node server.js"
  },
  "keywords": [
    "nodejs",
    "express",
    "restful",
    "api"
  ],
  "author": "Nicolás Arias",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^8.2.0"
  },
  "devDependencies": {
    "eslint": "^6.5.1",
    "eslint-config-airbnb-base": "^14.0.0",
    "eslint-plugin-import": "^2.18.2",
    "nodemon": "^1.19.4"
  }
}
